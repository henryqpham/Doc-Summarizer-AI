# secrets — NEVER commit
.env
.env.*
!.env.example

# dependencies (there are none, but just in case)
node_modules/

# dead build output from the v0.1 single-file architecture
dist/

# misc
.DS_Store
*.log
